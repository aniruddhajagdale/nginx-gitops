replicaCount: 1

service:
  type: ClusterIP

ingress:
  enabled: true
  ingressClassName: traefik
  hostname: nginx.local

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi